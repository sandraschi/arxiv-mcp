# arxiv-mcp — MCPB bundle root

This directory is the pack root for `mcpb pack` (Claude Desktop .mcpb).
arXiv research server: discovery, DOI resolution, LanceDB hybrid RAG, deep epistemic profiling.

## Build

From repository root:
```powershell
just mcpb-pack
```
